# 客户端鉴权 token 列表 (必填)
# 格式: name1:key1,name2:key2,...
# 生成 token: python3 -c 'import secrets; print("sk-myproxy-"+secrets.token_urlsafe(24))'
PROXY_API_KEYS=alice:sk-myproxy-REPLACE_ME_1,bob:sk-myproxy-REPLACE_ME_2

# 可选项,需要时取消注释
# PORT=5678
# BIND_HOST=0.0.0.0
# DEBUG=1
